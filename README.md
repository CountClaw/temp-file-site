# Temporary File Site

GitHub Pages static site for temporary file storage.

## Update files

1. Put files under `files/`
2. `git add . && git commit -m "update files" && git push`
3. Wait ~1 min for Pages refresh
